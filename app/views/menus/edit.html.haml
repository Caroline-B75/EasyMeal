-# UC1/UC2 — Édition des paramètres du menu
-# Brouillon : formulaire complet (régime, personnes, repas, nom) → re-génération
-# Actif/Archivé : formulaire simple (nom, personnes)
- content_for :title, "Modifier le menu"

= context_bar_back(menu_path(@menu), "Retour au menu")

- if @menu.status_draft?
  .mn-page-wrap
    .mn-page-header
      .mn-page-kicker
        %span.mn-page-kicker-sep
        Modifier les paramètres
      %h1.mn-page-title Régénérer le menu
      %p.mn-page-sub Modifie les paramètres ci-dessous. Le menu sera re-généré avec de nouvelles recettes.

    = render "params_form", menu: @menu, url: regenerate_menu_path(@menu),
                            submit_label: "Re-générer mon menu",
                            submit_note: "Les recettes actuelles seront remplacées. De nouvelles recettes seront sélectionnées en priorité parmi celles de saison.",
                            confirm: "Le menu actuel sera remplacé par un nouveau menu. Continuer ?"

- else
  .mn-hero
    .mn-hero-inner
      .mn-kicker
        %span.mn-kicker-line
        MODIFIER LE MENU
      %h1.mn-hero-title= @menu.name.presence || "Menu sans nom"

  .mn-body
    = render "form_edit", menu: @menu
