<div class="auth-links">
  <%- if controller_name != 'sessions' %>
    <p class="auth-link-text">
      Tu as déjà un compte ?
      <%= link_to "Me connecter", new_session_path(resource_name), class: "auth-link" %>
    </p>
  <% end %>

  <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
    <p class="auth-link-text">
      Pas encore de compte ?
      <%= link_to "Créer mon compte", new_registration_path(resource_name), class: "auth-link" %>
    </p>
  <% end %>

  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
    <p class="auth-link-text">
      <%= link_to "Mot de passe oublié ?", new_password_path(resource_name), class: "auth-link-secondary" %>
    </p>
  <% end %>

  <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <p class="auth-link-text">
      <%= link_to "Tu n'as pas reçu les instructions de confirmation ?", new_confirmation_path(resource_name), class: "auth-link-secondary" %>
    </p>
  <% end %>

  <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <p class="auth-link-text">
      <%= link_to "Tu n'as pas reçu les instructions de déverrouillage ?", new_unlock_path(resource_name), class: "auth-link-secondary" %>
    </p>
  <% end %>

  <%- if devise_mapping.omniauthable? %>
    <%- resource_class.omniauth_providers.each do |provider| %>
      <%= button_to "Se connecter avec #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), data: { turbo: false }, class: "btn btn-oauth" %>
    <% end %>
  <% end %>
</div>
