- content_for :title, "Modifier l'ingrédient"

= context_bar_back(ingredients_path, "Retour à la liste")

.page-container
  .page-header
    %h1 Modifie ton ingrédient

  .form-container
    = render "form", ingredient: @ingredient
